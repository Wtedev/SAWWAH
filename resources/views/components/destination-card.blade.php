<div class="relative group rounded-xl overflow-hidden shadow-lg">
    <img src="/images/{{ $img }}" alt="{{ $name }}" class="w-full h-64 object-cover group-hover:scale-105 transition duration-500">
    <div class="absolute bottom-0 bg-black bg-opacity-60 w-full p-4 text-white text-xl font-bold">
        {{ $name }}
    </div>
</div>
